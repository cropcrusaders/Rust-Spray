name: RustSpray CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
    # 1️⃣ Checkout repository
    - name: Checkout
      uses: actions/checkout@v4

    # 2️⃣ 🔒 Verify Cargo.lock is up-to-date
    - name: Verify Cargo.lock
      run: |
        cargo generate-lockfile          # rewrites lock if needed
        if ! git diff --exit-code Cargo.lock; then
          echo "::error::Cargo.lock is stale. Run 'cargo generate-lockfile' and commit the change."
          exit 1
        fi

    # 3️⃣ Install stable Rust toolchain for host (x86_64)
    - name: Set up Rust (host)
      uses: dtolnay/rust-toolchain@v1
      with:
        toolchain: stable
        components: clippy,rustfmt

    # 4️⃣ Cache host build artifacts
    - name: Cache cargo (host)
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-host-${{ hashFiles('**/Cargo.lock') }}

    # 5️⃣ Clippy + fmt (optional static checks)
    - name: Lint (clippy)
      run: cargo clippy --all-targets --all-features -- -D warnings
    - name: Format check
      run: cargo fmt -- --check

    # 6️⃣ Run unit & integration tests on host
    - name: Unit tests (host)
      run: cargo test --all-targets --all-features

    # 7️⃣ Install cross-rs for cross-compilation
    - name: Install cross
      run: cargo install cross --git https://github.com/cross-rs/cross

    # 8️⃣ Cache AArch64 build artifacts
    - name: Cache cargo (aarch64)
      uses: actions/cache@v3
      with:
        path: |
          ~/.cross
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-aarch64-${{ hashFiles('**/Cargo.lock') }}

    # 9️⃣ Build release binary for Raspberry Pi (ARM64)
    - name: Build for aarch64-unknown-linux-gnu
      run: cross build --release --target aarch64-unknown-linux-gnu

    # 🔟 Upload the compiled ARM binary as an artifact
    - name: Upload binary
      uses: actions/upload-artifact@v4
      with:
        name: rustspray-aarch64
        path: target/aarch64-unknown-linux-gnu/release/rustspray
