FROM ghcr.io/cross-rs/armv7-unknown-linux-gnueabihf:0.2.5
RUN apt-get update && \
    apt-get install -y --no-install-recommends libopencv-dev pkg-config clang libclang-dev && \
    rm -rf /var/lib/apt/lists/*
